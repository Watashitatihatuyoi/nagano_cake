<h2>ジャンル編集</h2>
<%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
  <td><%= f.label :ジャンル名 %></td>
  <td><%= f.text_field :name %></td>
  <td><%= f.submit "変更を保存", class: "btn", style: "background-color: #64CCC5; color: white;" %></td>
<% end %>