<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="mb-4">
      <nav class="navbar navbar-expand-lg navbar-dark p-2 bg-light border-bottom border-dark">
        <div class="container">
          <div class="logo">
             <%= link_to root_path, class:'navbar-brand' do %><%= image_tag 'logo.png', :size => '250x200' %><% end %>
          </div>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse show" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if customer_signed_in? %>
                <%= render 'layouts/customer_signed_in' %>
              <% elsif admin_signed_in? %>
                <%= render 'layouts/admin_signed_in' %>
              <% else %>
                <%= render 'layouts/sign_out' %>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
    <footer class="bg-light border-top border-dark pt-5 pb-5 mt-4">
      <div class="container">
        <div class="row">
          <div class="col mx-auto">
            <h3>NAGANO CAKE</h3>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
